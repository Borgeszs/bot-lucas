# bot-lucas

Estrutura completa inicial do Bot Lucas (Discord.js v14).

1. Copie `.env.example` → `.env` e preencha TOKEN, CLIENT_ID, GUILD_ID, MONGODB_URI, PREFIX
2. `npm install`
3. `npm run deploy-commands` (usa GUILD_ID para deploy rápido em servidor de dev)
4. `npm start`
